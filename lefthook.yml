pre-commit:
  parallel: true
  jobs:
    - run: pnpm run eslint --fix {staged_files}
      glob: "*.{js,ts,jsx,tsx}"

    - run: pnpm run prettier --write --cache --ignore-unknown {staged_files}
      glob: "*"
