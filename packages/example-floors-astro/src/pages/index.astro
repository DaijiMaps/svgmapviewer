---
import { config } from '../utils/config'

import { SvgMapViewer } from '../components/SvgMapViewer/SvgMapViewer'
import Layout from '../layouts/Layout.astro';
---

<Layout title={config.title}>
	<SvgMapViewer client:only="react" {...config} />
</Layout>
