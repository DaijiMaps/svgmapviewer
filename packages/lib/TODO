- reorg zoom (no expand)
  - don't unexpand/expand
  - just set: container { overflow: hidden; } 
  - then do animation
  - and re-position

- kill expand/unexpand
  - everything is expanded now
  - just switching: expand=3 and expand=9

- optimize PAN
  - recenter does not need unexpand/expand

- extend size adjustment
  - extend wider on mobile

- change Footer to not use `mode'
  - don't re-render; style-only
  - kill all clsx() uses

- touch context menu
  - horizontal?

- mini map viewer
