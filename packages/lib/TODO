- reorg zoom (no expand)
  - don't unexpand/expand
  - kill Pointer state

- kill expand/unexpand
  - expand right after resize

- optimize PAN
  - recenter does not need unexpand/expand

- extend size adjustment
  - extend wider on mobile

- change Ui to not use `mode'
  - don't re-render; style-only
  - kill all clsx() uses
  - mode change (esp. closing detail/balloon) should not change DOM!

- touch context menu
  - horizontal?

- mini map viewer

- fix gap by button-zoom during swipe

- fix homing
  - fix resize

- fix resize (== reset)
  - expandLayout from start

- make Ui use Style.tsx
  - click -> search -> update DOM (detail content) -> balloon animation
  - close doesn't need to change DOM; make it CSS style only
