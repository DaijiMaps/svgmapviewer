- expand size adjustment
  - expand wider on mobile

- change Ui to not use `mode'
  - don't re-render; style-only
  - kill all clsx() uses
  - mode change (esp. closing detail/balloon) should not change DOM!

- touch context menu
  - horizontal?

- mini map viewer

- fix gap by button-zoom during swipe

- make Ui use Style.tsx
  - click -> search -> update DOM (detail content) -> balloon animation
  - close doesn't need to change DOM; make it CSS style only

- handle mobile chrome address bar auto hiding
  - just accept height-only change

- optimize animation / rendering performance
  - visilibity?

- optimize CSS styling performance
